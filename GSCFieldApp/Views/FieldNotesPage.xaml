<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GSCFieldApp.Views.FieldNotesPage"
             xmlns:viemodel="clr-namespace:GSCFieldApp.ViewModel"
             xmlns:local="clr-namespace:GSCFieldApp.Services"
             xmlns:models="clr-namespace:GSCFieldApp.Models"
             x:DataType="viemodel:FieldNotesViewModel"
             Title="{local:Localize ShellFieldNotesTitle}">

    <ContentPage.Resources>

        <!-- Note Cards Template -->
        <DataTemplate x:Key="FieldNoteCard" x:DataType="models:FieldNote">
            <Frame Style="{StaticResource FieldAppNotesFrame}" HeightRequest="75" WidthRequest="150">
                <VerticalStackLayout Margin="5" >
                    <Label
                                Text="{Binding Display_text_1}"
                                FontAttributes="Bold" />
                    <Label 
                                Text="{Binding Display_text_2}"
                                FontAttributes="Italic"
                                VerticalOptions="End" />
                </VerticalStackLayout>

            </Frame>
        </DataTemplate>

    </ContentPage.Resources>

    <ScrollView Orientation="Vertical">

        <VerticalStackLayout VerticalOptions="StartAndExpand">
            
            <!--Station records-->
            <Button Text="Stations" BackgroundColor="{StaticResource FieldStationColor}" Style="{StaticResource FieldAppNoteHeaderButton}"
                    Command="{Binding HideCommand}" CommandParameter="Station"/>
            
            <FlexLayout BindableLayout.ItemTemplate="{StaticResource FieldNoteCard}" BindableLayout.ItemsSource="{Binding Stations}" 
                        Style="{StaticResource FieldAppFormFlexLayout}" IsVisible="{Binding IsStationVisible,Mode=TwoWay}"/>
            <Label Text="{Binding Stations.Count, StringFormat='Total records: {0:D}'}" Margin="0,0,0,10" />

            <!--Earth material records-->
            <Button Text="Earth Materials" BackgroundColor="{StaticResource FieldEarthMaterialColor}" Style="{StaticResource FieldAppNoteHeaderButton}"/>
            <FlexLayout BindableLayout.ItemTemplate="{StaticResource FieldNoteCard}" BindableLayout.ItemsSource="{Binding EarthMats}"
                        Style="{StaticResource FieldAppFormFlexLayout}"/>
            <Label Text="{Binding EarthMats.Count, StringFormat='Total records: {0:D}'}" Margin="0,0,0,10" />

            <!--Sample records-->
            <Button Text="Sample" BackgroundColor="{StaticResource FieldSampleColor}" Style="{StaticResource FieldAppNoteHeaderButton}" />
            <FlexLayout BindableLayout.ItemTemplate="{StaticResource FieldNoteCard}" BindableLayout.ItemsSource="{Binding Samples}"
                        Style="{StaticResource FieldAppFormFlexLayout}"/>
            <Label Text="{Binding Samples.Count, StringFormat='Total records: {0:D}'}" Margin="0,0,0,10" />

        </VerticalStackLayout>
        
    </ScrollView>

    <!--<FlexLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
        <CollectionView ItemsSource="{Binding FieldNotes}" BackgroundColor="AliceBlue" 
                        CanReorderItems="True" HorizontalOptions="StartAndExpand" SelectionMode="Single" IsGrouped="true">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Horizontal"
                           ItemSpacing="20" />
            </CollectionView.ItemsLayout>

            <CollectionView.GroupHeaderTemplate > 
                <DataTemplate x:DataType="models:FieldNoteGroup">

                    <Label Text="{Binding Name}" BackgroundColor="{StaticResource Primary}" 
                        TextColor="White"
                        FontSize="18"
                        FontAttributes="Bold" />


                </DataTemplate>
            </CollectionView.GroupHeaderTemplate>
            
            <CollectionView.ItemTemplate >
                <DataTemplate x:DataType="models:FieldNote">

                        <Frame Style="{StaticResource FieldAppNotesFrame}" HeightRequest="100">
                            <VerticalStackLayout Margin="10" >
                                <Label
                                   Text="{Binding Display_text_1}"
                                   FontAttributes="Bold" />
                                <Label 
                                   Text="{Binding Display_text_2}"
                                   FontAttributes="Italic"
                                   VerticalOptions="End" />
                            </VerticalStackLayout>
                            
                        </Frame>

                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.GroupFooterTemplate>
                <DataTemplate x:DataType="models:FieldNoteGroup">
                    <Label Text="{Binding Count, StringFormat='Total records: {0:D}'}"
                   Margin="0,0,0,10" />
                </DataTemplate>
            </CollectionView.GroupFooterTemplate>
            
        </CollectionView>
    </FlexLayout>-->
</ContentPage>