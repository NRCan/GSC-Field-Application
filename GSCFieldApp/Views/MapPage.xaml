<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GSCFieldApp.Views.MapPage"
             xmlns:mapsui="clr-namespace:Mapsui.UI.Maui;assembly=Mapsui.UI.Maui"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             xmlns:local="clr-namespace:GSCFieldApp.Services"
             xmlns:viewmodel="clr-namespace:GSCFieldApp.ViewModel"
             x:DataType="viewmodel:MapViewModel" 
             Title="{local:Localize ShellMapTitle}">
    <ContentPage.Content>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            
            <!--Controls header bar-->
            <StackLayout Orientation="Horizontal" BackgroundColor="{StaticResource Primary}" Grid.Row="0" FlowDirection="RightToLeft">

                <!--Manual XY-->
                <Button Text="XY" FontSize="Title" >
                </Button>

                <!--Waypoint-->
                <Button>
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF023D;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

                <!--Quick structure/pflow-->
                <Button>
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF183F;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

                <!--Quick photo-->
                <Button>
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF0D5D;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

                <!--Quick sample-->
                <Button>
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF04FD;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

                <!--Quick station-->
                <Button Command="{Binding AddStationCommand}">
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF12FC;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

            </StackLayout> 
            
            
            <!--Map Control-->
            <mapsui:MapView x:Name="mapView"
                            Grid.Row ="1"
                            IsVisible="true"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand"
                            Margin="10" />
            
            <!--Label Bar-->
            <Label Grid.Row="2"  x:Name="info" 
            HorizontalOptions="CenterAndExpand"
            Text="unknown" />
            
            <!--Controls bottom bar-->
            <StackLayout Orientation="Horizontal" BackgroundColor="{StaticResource Primary}" Grid.Row="3" FlowDirection="RightToLeft">

                <!--Tap option -->
                <Button Text="">
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF0A55;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>

                <!--Manage layers-->
                <Button Text="">
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF0F59;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>
                <!--Add new layer-->
                <Button Text="">
                    <Button.ImageSource>
                        <FontImageSource Glyph="&#xF0E4D;" FontFamily="MatDesign" />
                    </Button.ImageSource>
                </Button>
            </StackLayout>
        </Grid>
    </ContentPage.Content>

</ContentPage>